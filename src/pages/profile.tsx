import { HeadingTag } from '../components/headingTag';

const Profile = () => {
    return (
        <div>
            <HeadingTag>Profile</HeadingTag>
        </div>
    );
};

export default Profile;
